# Extra dependencies for INFERENCE_BACKEND=local — only install these on a box
# that actually has an NVIDIA GPU. call2fly runs the Modal backend and must NOT
# install these: torch alone is several GB and that server runs tight on disk.
#
# Install torch from the CUDA index first so you get a GPU build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r backend/requirements-gpu.txt
#
# cu121 is chosen over newer bundles because the target GPU is a Turing card
# (RTX 2080 Ti, compute 7.5) and cu121 has well-tested kernels for it; the
# driver (580.x, CUDA 13 capable) is backward compatible with it.

ultralytics>=8.3.0
huggingface_hub>=0.24.0
